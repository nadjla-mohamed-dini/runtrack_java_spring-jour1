spring:
  profiles:
      active: dev
---
spring: 
  config:
    activate:
      on-profile: dev

greeting: 
  message: "Hi there from dev"
---
spring: 
  config:
    activate:
      on-profile: prod

greeting:
  message: "Welcome to production"
